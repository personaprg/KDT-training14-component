23.11.28 회의 내용

1. for : 순회 방법을 사용한 이유는 엔드포인트를 개념이 아니다. 즉 string값으로 엔드포인트를 찾고 가지를 뻦어나가는 개념에 더하여 상응되면 안된다.
2. render : 이 기능을 설명하기 위해 내가 재귀함수의 개념을 알아야한다. if===string 특히 for의 반복, 순회 이런 개념으로만 사용했던 고정관념이 그 방법만을통해 해결하려고 스스로 옥죄이는 행위이다.
3. state 데이터와 itema 데이터의 정확함 값들의 정확한 데이터를 명시되어야 한다. 
이는 for문으로 순회할때 마지막 배열의 값에 "...children"이러한 개념의 사용법이 다 다르기 때문이다.
4. return document.createTextNode(virtualDom) 이 함수에서 string값이 객체로 변환되어야 하는 이유를 파악하고 있어야 한다.
